%{
#include <stdio.h>
%}
%%
[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.(com|org|net|edu|in)\b[ \t]*\n  { printf("VALID\n"); }
.*\n                                                            { printf("INVALID\n"); }
%%
int main(){ yylex(); }
